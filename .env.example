# Public (safe to expose to the browser bundle)
NEXT_PUBLIC_MAPBOX_TOKEN=

# IBM Quantum (server-side only)
# Required to run on IBM Quantum hardware
IBM_QUANTUM_INSTANCE_CRN=

# Optional: if not set, Qiskit may use stored credentials
QISKIT_IBM_TOKEN=
